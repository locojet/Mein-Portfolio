{
  "name": "mi-repertorio.0.1",
  "version": "1.0.0",
  "description": "Mein Portfolio",
  "main": "index.html",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "copyfiles -u 1 src/**/* dist/",
    "deploy": "gh-pages -d dist",
    "update": "git add . && git commit -m 'Automatisiertes Update' && git pull origin gh-pages && git push origin gh-pages && npm run build && npm run deploy"
  },
  "keywords": [
    "\"Javascript\"",
    "\"J.Query\"",
    "\"Webanwendungen\""
  ],
  "author": "Juan Carlos Mejia",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "copyfiles": "^2.4.1"
  }
}
